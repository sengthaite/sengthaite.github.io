import 'package:flutter/material.dart';

class HttpViewMobile extends StatelessWidget {
  const HttpViewMobile({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
