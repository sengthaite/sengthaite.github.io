import 'package:flutter/material.dart';

class CalculatorMobileView extends StatelessWidget {
  const CalculatorMobileView({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
