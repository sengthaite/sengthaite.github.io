import 'package:flutter/widgets.dart';

class APIEncryptionView extends StatelessWidget {
  const APIEncryptionView({super.key});

  @override
  Widget build(Object context) {
    return Container();
  }
}
