import 'package:flutter/widgets.dart';

class APIDynamicVariableView extends StatelessWidget {
  const APIDynamicVariableView({super.key});

  @override
  Widget build(Object context) {
    return Container();
  }
}
